diff --git a/dist/po-gettext.cjs b/dist/po-gettext.cjs
index ac4b19681dda4b4e8ac9f9c398a790cc67d14037..b1c2e3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d 100644
--- a/dist/po-gettext.cjs
+++ b/dist/po-gettext.cjs
@@ -72,6 +72,13 @@ function getCldrPluralSamples() {
 }
 
 const cldrSamples = getCldrPluralSamples();
+function formatPoGettextPluralDebug(value) {
+  try {
+    return JSON.stringify(value);
+  } catch {
+    return Object.prototype.toString.call(value);
+  }
+}
 function stringifyICUCase(icuCase) {
   return icuCase.tokens.map((token) => {
     if (token.type === "content") {
@@ -80,9 +97,28 @@ function stringifyICUCase(icuCase) {
       return "#";
     } else if (token.type === "argument") {
       return "{" + token.arg + "}";
+    } else if (token.type === "plural") {
+      const clauses = token.cases.map(
+        (c) => c.key + " {" + stringifyICUCase(c) + "}"
+      ).join(" ");
+      return "{" + token.arg + ", plural, " + clauses + "}";
+    } else if (token.type === "select") {
+      const clauses = token.cases.map(
+        (c) => c.key + " {" + stringifyICUCase(c) + "}"
+      ).join(" ");
+      return "{" + token.arg + ", select, " + clauses + "}";
+    } else if (token.type === "selectordinal") {
+      const clauses = token.cases.map(
+        (c) => c.key + " {" + stringifyICUCase(c) + "}"
+      ).join(" ");
+      return "{" + token.arg + ", selectordinal, " + clauses + "}";
     } else {
       console.warn(
-        `Unexpected token "${token}" while stringifying plural case "${icuCase}". Token will be ignored.`
+        `Unexpected token while stringifying plural case (token will be ignored).
+  token.type: ${token?.type}
+  token: ${formatPoGettextPluralDebug(token)}
+  icuCase.keys: ${icuCase && typeof icuCase === "object" ? Object.keys(icuCase).join(", ") : icuCase}
+  icuCase: ${formatPoGettextPluralDebug(icuCase)}`
       );
       return "";
     }
diff --git a/dist/po-gettext.mjs b/dist/po-gettext.mjs
index c42b71e6ed944ae18130404344695355ba1f7e61..d2c3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1 100644
--- a/dist/po-gettext.mjs
+++ b/dist/po-gettext.mjs
@@ -63,6 +63,13 @@ function getCldrPluralSamples() {
 }
 
 const cldrSamples = getCldrPluralSamples();
+function formatPoGettextPluralDebug(value) {
+  try {
+    return JSON.stringify(value);
+  } catch {
+    return Object.prototype.toString.call(value);
+  }
+}
 function stringifyICUCase(icuCase) {
   return icuCase.tokens.map((token) => {
     if (token.type === "content") {
@@ -71,9 +88,28 @@ function stringifyICUCase(icuCase) {
       return "#";
     } else if (token.type === "argument") {
       return "{" + token.arg + "}";
+    } else if (token.type === "plural") {
+      const clauses = token.cases.map(
+        (c) => c.key + " {" + stringifyICUCase(c) + "}"
+      ).join(" ");
+      return "{" + token.arg + ", plural, " + clauses + "}";
+    } else if (token.type === "select") {
+      const clauses = token.cases.map(
+        (c) => c.key + " {" + stringifyICUCase(c) + "}"
+      ).join(" ");
+      return "{" + token.arg + ", select, " + clauses + "}";
+    } else if (token.type === "selectordinal") {
+      const clauses = token.cases.map(
+        (c) => c.key + " {" + stringifyICUCase(c) + "}"
+      ).join(" ");
+      return "{" + token.arg + ", selectordinal, " + clauses + "}";
     } else {
       console.warn(
-        `Unexpected token "${token}" while stringifying plural case "${icuCase}". Token will be ignored.`
+        `Unexpected token while stringifying plural case (token will be ignored).
+  token.type: ${token?.type}
+  token: ${formatPoGettextPluralDebug(token)}
+  icuCase.keys: ${icuCase && typeof icuCase === "object" ? Object.keys(icuCase).join(", ") : icuCase}
+  icuCase: ${formatPoGettextPluralDebug(icuCase)}`
       );
       return "";
     }
