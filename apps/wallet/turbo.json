{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["$TURBO_EXTENDS$", "lingui"],
            "cache": false
        },
        "lingui": {
            "outputs": ["src/locales/**/*.ts"],
            "inputs": ["src/locales/**/*.po"]
        }
    }
}
