/**
 * Stub for optional wallet-connector peer dependencies that are not
 * installed. They are only reachable through client-side connector flows
 * that never run during SSR; wrangler's bundler just needs a resolution.
 */
export default {};
