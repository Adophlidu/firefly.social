export enum SolanaChainId {
    Mainnet = 101,
    Testnet = 102,
    Devnet = 103,
    // For any chains not supported yet.
    Invalid = 0,
}

export enum SolanaSchemaType {
    Native = 1,
    Fungible = 2,
    NonFungible = 3,
}
